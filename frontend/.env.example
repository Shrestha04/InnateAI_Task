# Leave empty for local dev (Vite proxies /api, /images, /static to the
# backend on :8000 — see vite.config.ts). Set to your deployed backend's
# base URL (no trailing slash) when the frontend is hosted separately, e.g.
# on Vercel with the FastAPI backend deployed elsewhere.
VITE_API_BASE=
